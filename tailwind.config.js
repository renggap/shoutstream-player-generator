/** @type {import('tailwindcss').Config} */
// Tailwind v4 configuration is primarily done via @theme in CSS
// This file is kept for IDE support and legacy compatibility
export default {
  // Content scanning is automatic in v4 when using Vite plugin
  // darkMode is handled automatically with .dark class in v4
  theme: {
    extend: {},
  },
  plugins: [],
}

